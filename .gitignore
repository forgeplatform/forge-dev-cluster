# Vagrant runtime state (per-VM IDs, sshd keys, etc.)
.vagrant/

# Runtime scratch shared with the VMs at /vagrant — kubeconfig tokens,
# join tokens, side-loaded image tarballs, copied chart sources, sample
# CRs. Repopulated on each provisioning / test cycle (mkdir -p in
# scripts/server-init.sh).
shared/

# OS / editor
.DS_Store
*.swp
*~
.idea/
.vscode/

# Misc logs
*.log
